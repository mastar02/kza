# Home Assistant Voice - Environment Variables
# Copy this file to .env and fill in your values

# ==================== Home Assistant ====================
# Your Home Assistant instance URL
HOME_ASSISTANT_URL=http://192.168.1.x:8123

# Long-lived access token from Home Assistant
# Create at: Settings > Security > Long-lived access tokens
HOME_ASSISTANT_TOKEN=your_long_lived_token_here

# ==================== GPU Configuration ====================
# GPU 0: STT (distil-whisper-large-v3-es)
# GPU 1: Embeddings (BGE-M3) + Speaker ID (ECAPA) + Emotion (wav2vec2)
# GPU 2: Router (Qwen2.5-7B-Instruct-AWQ via vLLM)
# GPU 3: TTS (Kokoro-82M + Qwen3-TTS-0.6B)
# CPU:   LLM (Qwen2.5-72B-Instruct Q6_K via llama-cpp)
CUDA_VISIBLE_DEVICES=0,1,2,3

# ==================== Configuration ====================
# Path to main configuration file
CONFIG_PATH=config/settings.yaml

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ==================== Spotify ====================
# Create app at: https://developer.spotify.com/dashboard
# Add redirect URI: http://localhost:8888/callback
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# ==================== Reasoner cloud (MiniMax) ====================
# Slow path reasoner — MiniMax-M2.7-highspeed vía api.minimax.io.
# Cargar en /home/kza/secrets/.env (chmod 600) y referenciar vía
# systemd EnvironmentFile= en kza-voice.service.
MINIMAX_API_KEY=

# Gateway LiteLLM que enruta al reasoner cloud (config/settings.yaml:
# reasoner.http_base_url y code_index.cards.base_url). Mismo host físico
# que kza-voice — si falta esta var, ambos servicios caen a
# http://127.0.0.1:8200/v1 (loopback) con un WARNING en el log, no rompen
# el boot. Setearla igual: el fallback NO es neutral en privacidad si
# compaction.enabled=true (config actual, ver settings.yaml) — sin esta
# var, el compactor recibe el placeholder "${LLM_GATEWAY_URL}" sin
# resolver, httpx.URL(...) tira UnsupportedProtocol y queda compactor=None
# (cero egreso de red); CON el fallback, recibe una URL local válida y SÍ
# intenta conectar. Detalle en src/main.py:322-332.
LLM_GATEWAY_URL=http://192.168.1.2:8200/v1
