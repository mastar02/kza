name: KZA CI/CD

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]
  workflow_dispatch:
  schedule:
    # 07:17 UTC diario, fuera de :00/:30 (evita picos de carga compartida
    # en runners de GitHub). Sin relacion con el cron de entrenamiento real
    # del server (ese vive en systemd del server, no en este workflow);
    # esto solo destraba el job nightly-training-test, que hoy nunca corre
    # porque su `if:` exige schedule/workflow_dispatch y `on:` no declaraba
    # ninguno de los dos (29/29 runs skipped).
    - cron: '17 7 * * *'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.13"

jobs:
  # ============================================================
  # LINT AND FORMAT CHECK
  # ============================================================
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Temporal: black --check e isort --check-only se contradicen sin config
    # compartida, asi que este job es hoy insatisfacible. Task 3 lo deja
    # solo en el sub-chequeo de formato una vez resuelto el conflicto.
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install linting tools
        run: |
          pip install ruff black isort mypy

      - name: Run ruff
        run: ruff check src/ tests/

      - name: Check formatting with black
        run: black --check src/ tests/

      - name: Check import order
        run: isort --check-only src/ tests/

  # ============================================================
  # UNIT TESTS
  # ============================================================
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libportaudio2

      - name: Install dependencies
        run: pip install -r requirements-test.txt

      - name: Collect tests
        run: python -m pytest tests/ --collect-only -q

      - name: Run unit tests
        run: |
          pytest tests/unit/ -v --cov=src --cov-report=xml --cov-report=term

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          fail_ci_if_error: false

  # ============================================================
  # SAFETY TESTS (CRITICAL)
  # ============================================================
  safety-tests:
    name: Safety Tests (CRITICAL)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libportaudio2

      - name: Install dependencies
        run: pip install -r requirements-test.txt

      - name: Collect tests
        run: python -m pytest tests/ --collect-only -q

      - name: Run safety tests
        run: |
          pytest tests/safety/ -v --tb=long
        # Safety tests MUST pass - no continue-on-error

  # ============================================================
  # INTEGRATION TESTS
  # ============================================================
  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [unit-tests, safety-tests]
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install system deps
        run: sudo apt-get update && sudo apt-get install -y libportaudio2

      - name: Install dependencies
        run: pip install -r requirements-test.txt

      - name: Collect tests
        run: python -m pytest tests/ --collect-only -q

      - name: Run integration tests
        run: |
          pytest tests/integration/ -v --tb=short

  # ============================================================
  # SECURITY SCAN
  # ============================================================
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Temporal: el Task 2 saca este continue-on-error una vez resuelto lo
    # que hoy hace fallar a bandit -ll (severidad media+).
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install security tools
        run: |
          pip install bandit safety

      - name: Run bandit security scan
        run: |
          bandit -r src/ -f json -o bandit-report.json || true
          bandit -r src/ -ll  # Fail on medium+ severity

      - name: Check dependencies for vulnerabilities
        run: |
          pip install -r requirements.txt

  # ============================================================
  # BUILD CHECK
  # ============================================================
  build:
    name: Build Check
    runs-on: ubuntu-latest
    needs: [unit-tests, safety-tests]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Verify imports work
        run: |
          python -c "from src.pipeline import ModelManager, VoicePipeline"
          python -c "from src.training import NightlyTrainer, NightlyConfig"
          python -c "from src.alerts import AlertManager"
          echo "✅ All imports successful"

      - name: Check configuration
        run: |
          python -c "import yaml; yaml.safe_load(open('config/settings.yaml'))"
          echo "✅ Configuration valid"

  # ============================================================
  # NIGHTLY TRAINING TEST (Scheduled)
  # ============================================================
  nightly-training-test:
    name: Nightly Training Simulation
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio

      - name: Simulate nightly training cycle
        run: |
          python -c "
          import asyncio
          from src.training import NightlyTrainer, NightlyConfig

          async def test_cycle():
              config = NightlyConfig(
                  min_samples_to_train=1,
                  gpus=[0],
                  use_qlora=True
              )
              trainer = NightlyTrainer(config)

              # Test data collection
              examples, stats = trainer.collect_daily_data()
              print(f'Collected {len(examples)} examples')

              # Test status
              status = trainer.get_status()
              print(f'Status: {status}')

              print('✅ Nightly training simulation passed')

          asyncio.run(test_cycle())
          "
